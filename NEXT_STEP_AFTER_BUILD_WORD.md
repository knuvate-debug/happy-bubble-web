# Next Step After BUILD_WORD

다음 추천 구현:

```txt
1. S3 SOUND_MATCH preview 연결 확인
2. /teacher/s02, /teacher/s03 preview 흐름 준비
3. Supabase event dashboard in /admin
4. QR production/preview 관리 UI
```

가장 추천:

```txt
S3 SOUND_MATCH preview + GameRouter 확장 검수
```

이유:

```txt
S1 SOUND_MATCH와 S2 BUILD_WORD가 모두 Phaser로 구현되었으므로,
S3 SOUND_MATCH 데이터가 GameRouter에서 재사용되는지 확인하면 공통 엔진 구조가 검증됩니다.
```
